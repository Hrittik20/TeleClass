═══════════════════════════════════════════════════════════════
  TELEGRAM LMS - GITHUB DEPLOYMENT (Quick Reference)
═══════════════════════════════════════════════════════════════

📋 CHECKLIST - Do this in order:

PART 1: LOCAL (On Your Computer)
─────────────────────────────────────────────────────────────

1. Get Bot Token
   □ Open Telegram, search @BotFather
   □ Send: /newbot
   □ Follow prompts, copy token

2. Create GitHub Repository
   □ Go to: github.com/new
   □ Create new repository
   □ Copy repository URL

3. Push Code
   □ git init
   □ git add .
   □ git commit -m "Initial commit"
   □ git remote add origin YOUR_GITHUB_URL
   □ git push -u origin main

✅ Code is now on GitHub!


PART 2: SERVER SETUP (On Your Server)
─────────────────────────────────────────────────────────────

4. Get a Server (Choose One)
   Option A: DigitalOcean ($6/month)
   □ Sign up at digitalocean.com
   □ Create Droplet (Ubuntu 22.04, 1GB RAM)
   □ Copy IP address
   
   Option B: AWS EC2 (Free tier)
   □ Launch t2.micro instance
   □ Ubuntu 22.04
   □ Copy public IP

5. Connect to Server
   □ ssh root@YOUR_SERVER_IP
   □ (or) ssh -i key.pem ubuntu@YOUR_SERVER_IP

6. Install Requirements
   □ sudo apt update
   □ sudo apt install python3 python3-pip python3-venv git nginx certbot -y

7. Clone and Deploy
   □ git clone YOUR_GITHUB_URL lms
   □ cd lms
   □ chmod +x deploy.sh
   □ ./deploy.sh

8. Configure .env
   □ nano .env
   □ Set: NOETICA_BOT_TOKEN=your_token
   □ Set: WEBAPP_URL=https://your-domain.com/miniapp
   □ Save: Ctrl+X, Y, Enter

9. Set Up Nginx (if using domain)
   See: GITHUB_DEPLOYMENT.md - Section E
   □ Configure Nginx
   □ Get SSL: sudo certbot --nginx -d your-domain.com

10. Configure Bot
    □ Open @BotFather in Telegram
    □ /mybots → Select bot → Bot Settings → Menu Button
    □ Set URL: https://your-domain.com/miniapp

✅ Everything is deployed!


PART 3: TEST (In Telegram)
─────────────────────────────────────────────────────────────

11. Test Bot
    □ Search for your bot
    □ Send: /start
    □ Should get welcome message

12. Test Mini App
    □ Send: /dashboard
    □ Click button
    □ Mini App should load

13. Create Test Class
    □ Create Telegram group
    □ Add bot to group
    □ Send: /init_class
    □ Note the course code

14. Create Assignment
    □ Open /dashboard
    □ Create assignment
    □ Check if posted to group

✅ LMS is working!


═══════════════════════════════════════════════════════════════
  USEFUL COMMANDS (Save These!)
═══════════════════════════════════════════════════════════════

On Server:
──────────
# Check status
sudo systemctl status lms-api lms-bot

# Restart services
sudo systemctl restart lms-api lms-bot

# View logs
sudo journalctl -u lms-api -f
sudo journalctl -u lms-bot -f

# Update from GitHub
cd /var/www/lms
git pull
sudo systemctl restart lms-api lms-bot

# Backup data
cp /var/www/lms/data/data.json ~/backup/


═══════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

Bot not responding?
→ sudo systemctl restart lms-bot
→ sudo journalctl -u lms-bot -f

Mini App won't load?
→ Check: https://your-domain.com/api/health
→ Should return: {"ok":true}
→ sudo systemctl restart lms-api

Can't push to GitHub?
→ git config --global user.email "you@example.com"
→ git config --global user.name "Your Name"

Services won't start?
→ Check logs: sudo journalctl -u lms-api -n 50
→ Check .env: cat /var/www/lms/.env
→ Test manually: cd /var/www/lms && source venv/bin/activate && python bot/bot.py


═══════════════════════════════════════════════════════════════
  QUICK REFERENCES
═══════════════════════════════════════════════════════════════

Full Guide:        GITHUB_DEPLOYMENT.md
Quick Start:       README_FIRST.md
API Docs:          API_REFERENCE.md
All Features:      FEATURES.md
Architecture:      ARCHITECTURE.md


═══════════════════════════════════════════════════════════════
  COSTS
═══════════════════════════════════════════════════════════════

Server:      $6/month (DigitalOcean) or FREE (AWS first year)
Domain:      $10/year (optional)
SSL:         FREE (Let's Encrypt)
Bot:         FREE (Telegram)

Total:       ~$7/month (or FREE first year on AWS)


═══════════════════════════════════════════════════════════════
  SUPPORT
═══════════════════════════════════════════════════════════════

1. Check the documentation (10 guides available)
2. Search for error messages in guides
3. Check logs: sudo journalctl -u lms-api -f
4. Test API: curl http://localhost:8000/api/health


═══════════════════════════════════════════════════════════════

🎉 You're all set! Follow the checklist above step by step.

Need detailed instructions? Open: GITHUB_DEPLOYMENT.md

Time to deploy: ~1 hour total
Difficulty: Easy (just follow the steps!)

═══════════════════════════════════════════════════════════════

