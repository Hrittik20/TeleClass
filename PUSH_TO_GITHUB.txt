═══════════════════════════════════════════════════════════════
  PUSH YOUR TELEGRAM LMS TO GITHUB - COPY THESE COMMANDS
═══════════════════════════════════════════════════════════════

STEP 1: CREATE GITHUB REPOSITORY
─────────────────────────────────────────────────────────────

1. Go to: https://github.com/new

2. Fill in:
   Repository name: telegram-lms (or your choice)
   Description: Learning Management System for Telegram
   Privacy: Public or Private (your choice)
   
   ⚠️ DO NOT check "Initialize with README"
   ⚠️ DO NOT add .gitignore or license yet

3. Click "Create repository"

4. COPY the repository URL shown (looks like):
   https://github.com/YOUR_USERNAME/telegram-lms.git


STEP 2: RUN THESE COMMANDS (In PowerShell/Terminal)
─────────────────────────────────────────────────────────────

Open PowerShell in your C:\Git\Miniapp folder, then copy/paste:

# Initialize git (if not already done)
git init

# Add all files
git add .

# Check what will be committed
git status

# Commit everything
git commit -m "Initial commit - Telegram LMS with Mini App"

# Configure git (if first time)
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"

# Add your GitHub repository (REPLACE WITH YOUR URL!)
git remote add origin https://github.com/YOUR_USERNAME/telegram-lms.git

# Push to GitHub
git branch -M main
git push -u origin main


STEP 3: VERIFY
─────────────────────────────────────────────────────────────

1. Go to your GitHub repository page
2. You should see all your files there
3. Check that .env is NOT there (it's in .gitignore - good!)


✅ DONE! Your code is on GitHub!


═══════════════════════════════════════════════════════════════
  WHAT'S ALREADY PROTECTED
═══════════════════════════════════════════════════════════════

These files will NOT be pushed (already in .gitignore):
✅ .env (your secrets)
✅ data/ (your database and user files)
✅ __pycache__/ (Python cache)
✅ venv/ (virtual environment)

These WILL be pushed (safe to share):
✅ All Python code
✅ All documentation
✅ HTML/CSS/JS files
✅ requirements.txt
✅ Configuration templates (.env.example)


═══════════════════════════════════════════════════════════════
  NEXT STEPS AFTER PUSHING
═══════════════════════════════════════════════════════════════

After pushing to GitHub:

1. Go to your server (DigitalOcean, AWS, etc.)

2. Run these commands on your server:
   
   sudo apt update
   sudo apt install python3 python3-venv git -y
   git clone https://github.com/YOUR_USERNAME/telegram-lms.git lms
   cd lms
   chmod +x deploy.sh
   ./deploy.sh

3. When deploy.sh asks, create your .env file with:
   - Your bot token
   - Your domain URL

4. Continue with GITHUB_DEPLOYMENT.md guide


═══════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

Error: "failed to push some refs"
→ git pull origin main --allow-unrelated-histories
→ git push -u origin main

Error: "support for password authentication was removed"
→ Use Personal Access Token instead of password
→ GitHub Settings → Developer settings → Personal access tokens
→ Generate new token → Use as password

Error: "git: command not found"
→ Install git first:
   Download from: https://git-scm.com/download/win

Already have a git repo?
→ git remote set-url origin https://github.com/YOUR_USERNAME/telegram-lms.git
→ git push -u origin main


═══════════════════════════════════════════════════════════════

🎉 Ready to push? Copy the commands from STEP 2 above!

═══════════════════════════════════════════════════════════════

